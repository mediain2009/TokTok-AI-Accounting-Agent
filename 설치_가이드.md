# 톡톡AI,간편회계 — 설치 가이드

## 1. Flutter SDK 설치

1. https://docs.flutter.dev/get-started/install/windows 접속
2. "Download Flutter SDK" 클릭 후 zip 다운로드
3. `C:\flutter` 폴더에 압축 해제
4. 시스템 환경변수 PATH에 `C:\flutter\bin` 추가
5. 터미널에서 확인:
   ```
   flutter --version
   ```

## 2. Windows 데스크탑 빌드 활성화

```bash
flutter config --enable-windows-desktop
```

## 3. Visual Studio Build Tools 설치 (Windows 빌드에 필요)

https://visualstudio.microsoft.com/downloads/ 에서
**"Build Tools for Visual Studio"** 다운로드 후 설치
→ "Desktop development with C++" 워크로드 선택


## 4. 프로젝트 생성 및 파일 복사

```bash
# 1. Flutter 프로젝트 생성
flutter create --platforms=windows tax_invoice_flutter
cd tax_invoice_flutter

# 2. lib/ 폴더 전체를 제공된 lib/ 로 교체
#    (제공된 lib/ 파일들을 새로 생성된 lib/ 폴더에 복사)

# 3. pubspec.yaml 교체
#    (제공된 pubspec.yaml로 덮어쓰기)

# 4. 패키지 설치
flutter pub get
```


## 5. 실행

```bash
# 개발 모드 실행 (디버그)
flutter run -d windows

# 배포용 빌드 (.exe 생성)
flutter build windows
```

빌드 완료 후 실행 파일 위치:
```
build\windows\x64\runner\Release\tax_invoice.exe
```

---

## 자동 셋업 스크립트

`셋업.bat` 을 더블클릭하면 3~5단계를 자동으로 진행합니다.
(Flutter SDK가 PATH에 등록되어 있어야 합니다.)


## 데이터 저장 위치

데이터는 Windows 문서 폴더에 저장됩니다:
```
C:\Users\[사용자명]\Documents\TaxInvoice\invoices.db
```
이 파일 1개만 백업하면 모든 데이터가 보존됩니다.


## 화면 구성

| 화면 | 기능 |
|------|------|
| 발행 | 세금계산서 작성 / 종이·전자 발행 처리 |
| 매출조회 | 발행된 매출 세금계산서 조회, 수정발행 |
| 매입조회 | 매입 세금계산서 조회, 전자·종이 분류 |
| 합계표 | 거래처별 기수별 합계 (부가세 신고용) |
| 거래처 관리 | 거래처 CRUD |


## 문제 해결

**`flutter: command not found`**
→ Flutter SDK의 bin 폴더를 PATH에 추가했는지 확인

**`Error: No Windows desktop support`**
→ `flutter config --enable-windows-desktop` 실행 후 재시도

**빌드 오류: Visual Studio 관련**
→ Visual Studio Build Tools에서 "Desktop development with C++" 설치 확인

**`flutter doctor`** 명령으로 환경 상태를 전체 확인할 수 있습니다.
